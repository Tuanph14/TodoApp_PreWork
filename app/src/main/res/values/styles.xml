<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:actionBarStyle">@style/CustomActionBar</item>
        <item name="android:logo">@drawable/todoist_logo_detail</item>
        <item name="android:homeAsUpIndicator">@drawable/backbutton</item>
    </style>

    <style name="ListlyTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/CustomActionBar</item>
        <item name="android:logo">@drawable/todoist_logo_detail</item>
        <item name="android:homeAsUpIndicator">@drawable/backbutton</item>
    </style>



    <style name="CustomActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">#e6421c</item>

    </style>

</resources>
